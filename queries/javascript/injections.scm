;((jsx_context
;  (_) @injection.content)          ; capture the inner content, not the braces
; (#set! injection.language "javascript")
; (#set! injection.include-children) ; recurse into all children
; (#set! injection.combined))        ; merge with outer highlights (helps with overlap)
